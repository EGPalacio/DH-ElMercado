<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/4fa9f35e5d.js" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100;0,300;0,400;0,600;0,700;1,200&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/register.css">
    <title>Registro de usuarios</title>
</head>
<body>
    <div class="login-box">
        <div class="lb-logo">
          <a href="/"><img src="../images/logo-elMercado.png" alt="logo-El-Mercado"></a>
        </div>
        <div class="separador"></div>
        <div class="lb-header">
            <a href="/login"  id="login-box-link">INGRESAR</a>
            <a href="#"class="active"  id="register-box-link">REGISTRARSE</a>
          </div>
        <div class="social-login">
            <a href="#">
              <i class="fa fa-facebook fa-lg"></i>
              Login in with facebook
            </a>
            <a href="#">
              <i class="fa fa-google-plus fa-lg"></i>
              log in with Google
            </a>
          </div>
          <div class="form-box">
            <div class="errores">
              <% if (typeof errors != "undefined" ) { %>
           
                <ul>
                    <% errors.forEach(function(elemento){ %>
                      <li>
                        <%= elemento.msg %>
                      </li>
                    <% }) %>
                </ul>
              <% } %>
            </div>
           
        <form action="/register" class="lb-form" method="POST" enctype="multipart/form-data" >
        <input type="text" name="first_name" placeholder="Ingresá tu nombre">
        <input type="text" name="last_name" placeholder="Ingresá tu apellido">
        <input type="email" name="email" placeholder="Ingresá tu correo">
        <input type="password" name="password" placeholder="Ingresá una contraseña">
        <input type="password" name="password_repeat" placeholder="Ingresa nuevamente tu contraseña">
        <select name="user_type_id" class="custom-select" >
          <% for (let i=0; i < roles.length; i++) { %>
  
             
              <option value="<%=roles[i].id%>">
                  <%=roles[i].user_type%>
              </option>
  
        
         <% } %>
         
      </select>
        <input type="file"  id="avatar" name="avatar" placeholder="Ingresa un avatar para tu perfil" class="file" >
        <button type="submit" class="btn-registro">  REGISTRARME </button>
        
        </form>
    </div>
    </div>
</body>
</html>