<%- include('partials/head.ejs')%>
    </head>

    <body>
        <%- include('partials/header-logo.ejs')%>
            <%- include('partials/header-navbar.ejs')%>

                <% for (let userItem in user) { %>
                    <div class="input-group-prepend mb-2">
                        <label class="input-group-text text-left text-capitalize col-md-4"><%= `${userItem}:` %></label>
                        <input name="<%= userItem %>" type="text" readonly class="input-group form-control col-8" placeholder="<%= `${user[userItem]}` %>">
                    </div>
                    <% } %>

                        <body>

                        </body>

                        </html>